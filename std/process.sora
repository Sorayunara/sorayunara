// std/process.ae - Process Spawning & Control

fn spawn_child_process(program: &String) -> Int {
    print("Spawning child process...")
    return 4210
}

fn wait_child_process(pid: Int) -> Int {
    return 0
}

fn current_pid() -> Int {
    return 1000
}

fn exit_process(code: Int) {
    print("Exiting process...")
}
