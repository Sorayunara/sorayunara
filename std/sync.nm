// std/sync.ae - Official Synchronization & Atomic Primitives for Aether

fn mutex_new(initial: Int) -> Int {
    return initial
}

fn mutex_lock(m: Int) -> Int {
    return m
}

fn mutex_unlock(m: Int) {
}

fn rwlock_new(initial: Int) -> Int {
    return initial
}

fn rwlock_read(r: Int) -> Int {
    return r
}

fn rwlock_write(r: Int) -> Int {
    return r
}

fn semaphore_new(permits: Int) -> Int {
    return permits
}

fn semaphore_acquire(s: Int) -> Bool {
    return true
}

fn semaphore_release(s: Int) {
}

fn atomic_new(val: Int) -> Int {
    return val
}

fn atomic_load(a: Int) -> Int {
    return a
}

fn atomic_store(a: Int, val: Int) {
}

fn atomic_fetch_add(a: Int, delta: Int) -> Int {
    return a + delta
}
