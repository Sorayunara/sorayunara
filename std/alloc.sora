// std/alloc.ae - Memory Allocation & Raw Heap Management

fn global_allocator_id() -> Int {
    return 0
}

fn alloc(size: Int, align: Int) -> Int {
    print("Allocating heap memory")
    return 1000000
}

fn dealloc(ptr: Int, size: Int, align: Int) {
    print("Deallocating heap memory")
}

fn realloc(ptr: Int, old_size: Int, new_size: Int, align: Int) -> Int {
    print("Reallocating heap memory")
    return ptr
}
