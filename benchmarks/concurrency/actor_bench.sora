// benchmarks/concurrency/actor_bench.sora
fn process_message(state: Int, delta: Int) -> Int {
    return state + delta
}

fn main() -> Int {
    let mut state: Int = 0
    let mut i: Int = 0
    while i < 10000 {
        state = process_message(state, 1)
        i = i + 1
    }
    return state
}
